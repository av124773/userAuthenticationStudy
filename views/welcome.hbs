<div class="container">
  <div class="row">
    <div class="col-auto mx-auto mt-3">
      <h1>Welcome back, {{user.firstName}}!</h1>
    </div>
  </div>

  <div class="row">
    <div class="col-auto mx-auto">
      cookie:
      <div class="badge bg-primary text-wrap">
        {{#if cookieStatus}} id: {{user._id}} {{else}} no cookie {{/if}}
      </div>
    </div>
  </div>

  <div class="row">
    <div class="col-auto mx-auto mt-3">
        <a href="/logout">Logout</a>
    </div>
  </div>

</div>

